---
interface Props {
  title: string;
  subtitle?: string;
}
const { title, subtitle } = Astro.props;
---

<div class="mb-8">
  <h1 class="text-2xl font-extrabold text-[#e2e8f0] mb-1">{title}</h1>
  {subtitle && <p class="text-sm text-[#64748b]">{subtitle}</p>}
</div>
