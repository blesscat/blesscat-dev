---
interface Props {
  title: string
  subtitle?: string
}
const { title, subtitle } = Astro.props
---

<div class="mb-8">
  <h1 class="text-2xl font-extrabold text-text mb-1">{title}</h1>
  {subtitle && <p class="text-sm text-subtle">{subtitle}</p>}
</div>
