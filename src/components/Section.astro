---
interface Props {
  title?: string;
  class?: string;
}
const { title, class: className } = Astro.props;
---

<div class:list={['mt-10', className]}>
  {title && <h2 class="text-sm font-bold text-muted uppercase tracking-widest mb-4">{title}</h2>}
  <slot />
</div>
